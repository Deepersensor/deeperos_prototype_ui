import 'package:flutter/material.dart';

class AIModelUI extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('AI Model UI')),
      body: Center(child: Text('AI model interactions go here')),
    );
  }
}
