import 'package:flutter/material.dart';

class ActuatorsUI extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Actuators')),
      body: Center(child: Text('Actuator controls')),
    );
  }
}
